function areYouPlayingBanjo(name) {
    //this function takes in a name a checks to see if name starts  with 'r'
    return name.toLowerCase().startsWith('r')
      ? name + " plays banjo"
      : name + " does not play banjo";
  }
  //if name starts with 'r' return name + " plays banjo"
  //else name + " does not play banjo";
  